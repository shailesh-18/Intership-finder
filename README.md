# Intership-finder
Internship Finder is a Flask app that scrapes free internship listings, builds text embeddings, and ranks results using a hybrid recommendation engine. Search via a clean web UI or JSON APIs, filter by skills and location, and run everything locally using only open-source tools.
